# docs/

Design notes referenced from the top-level `README.md`.

## Files

- **`SCOPE.md`** - capability matrix and design notes. What PPSSPP's
  WebSocket debugger exposes (memory, CPU breakpoints, MIPS registers,
  framebuffer), what it doesn't (savestates, native frame-advance), and how
  this bridge compares to the sister Lua-based bridges (mGBA, BizHawk).
